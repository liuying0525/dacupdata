
    <view class="secRegister">
        <view class="header">
            <image src="{{url_link?url_link+'/loginLogo.png': ''}}"/>
            <view class="warnInfo">*仅限已申请东正金融贷款业务的客户</view>
        </view>
        <view class="inputBox">
            <input type="text" placeholder="请输入您的姓名" data-name="name" value="{{name}}" bindinput="changeValue"/>
        </view>
        <view class="inputBox">
            <input type="text" placeholder="请输入您的身份证号码" data-name="IdCard" value="{{IdCard}}" bindinput="changeValue"/>
        </view>
        <view class="inputBox">
            <input type="number" placeholder="请输入手机号" maxlength="11" data-name="phone" value="{{phone}}" bindinput="changeValue"/>
            <view class="errorInfo">*登录手机号须与贷款合同填写手机号保持一致</view>
        </view>
        <view class="inputBox">
            <input type="text" class="codeInput" placeholder="请输入验证码" data-name="code" value="{{code}}" bindinput="changeValue"/>
            <view class="codeBtn {{getCodeTime > 0 ? 'codeBtnNo' : ''}}" bindtap="getCode">{{getCodeTime > 0 ? getCodeTime + 's后可重新获取': '获取验证码'}}</view>
        </view>
        <view class="inputBox">
            <input type="password" placeholder="请设置8～20位字母和数字的组合的密码" data-name="password" value="{{password}}" bindinput="changeValue"/>
        </view>
        <view class="inputBox" style="margin-bottom: 26rpx;">
            <view class="button" bindtap="register">立即注册</view>
        </view>
        <view class="inputBox">
            <!--<text class="forget">立即注册</text>-->
            <text class="register" bindtap="goLogin">已有账号？去登录</text>
        </view>
    </view>