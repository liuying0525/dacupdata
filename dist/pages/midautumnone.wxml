
        <view>
            <view class="one">
              <image class="widget__arrow" src="{{topimgurl}}" mode="aspectFill"/>
            </view>
            <view class="join-top"  wx:if="{{floorName==''?false:true}}">
                <view class="join-top-rx">
                <image class="join-img" src="{{url_link?url_link+'tiao@2x.png':''}}"/>
                <text class="rxcx">{{floorName}}</text>
                  <image class="join-img" src="{{url_link?url_link+'tiao@2x.png':''}}"/>
                </view>
            </view>
           <view class="joio-div">
              <view class="joio-li-kuang"  wx:for="{{carmodel}}" wx:key="key" data-carmodelid="{{item.carmodelid}}" bindtap="toChanpinDetail">
                <view class="join-su">
                      <view class="joio-one">{{item.carseriesname}}</view>
                      <view class="joio-tow">
                        <image class="joio-img" src="{{item.thumbnailimgurl}}" mode="aspectFill"/>
                      </view> 
                </view>
              <view class="join-su">
                <view class="joio-three">{{item.name}}</view>
                    <view class="joio-four">
                      <text>首付{{item.downpayment/10000}}万</text><text>月供{{item.monthsuppert}}元</text>
                    </view>
              </view>
              </view>
            </view>
            <view class="zxs">
                <view class="zxkf"/>
            </view>
            <view class="zx" wx:if="{{lookatmore=='0'?false:true}}" bindtap="gengd" data-wpygengd-a="{{cartype}}">
              <view class="gengd">查看更多</view>
            </view>
        </view>