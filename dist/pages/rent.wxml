
  <view class="testresult">
    <view class="brand">
      
  <!--搜索-->
  <view class="index_ss_content">
    <view class="index_ss_header" bindsubmit="$inputd$formSubmit">
      <text class="index_input_ss iconfont icon-sousuo" bindtap="$inputd$mySelecwtc"/>
      <input class="index_ss" type="hidden" name="key" placeholder-class="placeholder" placeholder="请输入品牌或车型" bindconfirm="$inputd$my" bindinput="$inputd$userNameInput" value="{{$inputd$userName}}"/>
           <!-- <image class="{{isClearShow?'clearImgShow':'clearImgHide'}}" src='../pages/image/dingd.png' mode='widthFix'></image> -->
    </view>
  </view>

    </view>
    <view class="shosyi" hidden="{{selectPersons}}">
      <view class="bt" bindtap="communicate">热搜</view>
      <view class="rs">
        <text  wx:for="{{hotsear}}" wx:key="key" bindtap="brasd" data-index="{{item.name}}" data-hotsearchwordid="{{item.hotsearchwordid}}" id="{{item.name}}" data-wpybrasd-a="daikuan">{{item.name}}</text>
      </view>
      <view class="bt">历史记录
        <text bindtap="deleteHistory" class="iost iconfont  #icon-shanchu"/>
      </view>
      <view class="rs flo" hidden="{{sd}}">
        <text  wx:for="{{searchData}}" wx:key="key" bindtap="brad" data-index="{{item}}" id="{{item}}">{{item}}</text>
      </view>
    </view>
    <view class="ds" wx:if="{{textx.length > 0}}">
      <view class="liq" wx:for="{{textx}}" wx:key="key">
        <view class="li" bindtap="brad" data-index="{{item}}" id="{{item}}">{{item}}</view>
      </view>
      <view wx:if="{{code=='A00006'?true:false}}">
        <view class="kong" wx:if="{{textx.length=='0'?true:false}}">
          <image class="kong-image1" src="{{url_link?url_link + 'kong.png':''}}" mode="widthFix"/>
          <text class="kong-text">抱歉，暂无相关品牌或车系</text>
        </view>
      </view>
    </view>
  </view>