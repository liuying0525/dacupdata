
    <view class="earlyRepaymentS">
        <view class="earlyRepaymentSt">
            <view class="title">个人信息填写</view>
            <view class="textItem">
                <text>姓名</text><input type="text" value="{{name}}" bindinput="changeValue" disabled data-name="name" placeholder="请输入你的姓名"/>
            </view>
            <view class="textItem">
                <text>手机号</text><input type="text" value="{{phone}}" bindinput="changeValue" disabled data-name="phone" placeholder="请输入你填写合同时的手机号"/>
            </view>
            <!--<view class="textItem" style="margin-bottom: 50rpx;">-->
                <!--<text>合同信息</text><input type="text" bindtap="getContracts" disabled placeholder="点击获取合同列表" value="{{contractnum}}"/>-->
                <!--<view class="textItemInfos" wx:if="{{contract_show}}">-->
                    <!--<view class="textItemInfo" bindtap="selectContract({{item}})" wx:for="{{contracts}}">{{item.comments}} : {{item.externalContractNbr}}</view>-->
                <!--</view>-->
            <!--</view>-->
            <view class="textItem">
                <text>合同信息</text>
                <picker value="{{contractnum}}" range="{{contracts}}" bindchange="selectContract">
                    <view class="picker">
                        <text class="earlyTime">{{contractnum}}</text>
                    </view>
                </picker>
            </view>
            <view class="textItem">
                <text class="early" bindtap="earlyHelpInfo">提前还款时间 <text class="earlyHelp">?</text></text>
                <picker value="{{currentTime}}" range="{{timeList}}" bindtap="timeSelect" bindchange="timeChange" disabled="{{phone==''||contractnum==''}}">
                    <view class="picker">
                        <text class="{{!!fontColor?'colorRed':'earlyTime'}}">{{currentTime}}</text>
                    </view>
                </picker>
            </view>
            <!--<view class="textItem">-->
                <!--<text>短信验证码</text>-->
                <!--<view class="getCode {{phoneCodeTime>0 || canTryTime>0?'getNoCode':''}}" bindtap="getCode">{{phoneCodeTime>0?phoneCodeTime+'s后可重新获取': '获取验证码'}}</view>-->
                <!--<input class="phoneCode" type="text" value="{{phoneCode}}" data-name="phoneCode" bindinput="changeValue"/>-->
            <!--</view>-->
            <!--<view class="textItem" wx:if="{{picCode_show}}">-->
                <!--<text>图形验证码</text>-->
                <!--<view class="picCode">-->
                    <!--<image src="{{picCode_url}}" bindtap="getPic"></image>-->
                <!--</view>-->
                <!--<input class="phoneCode" type="text" value="{{picCode}}" data-name="picCode" bindinput="changeValue"/>-->
            <!--</view>-->
        </view>
        <view class="{{canTry?'earlyBtn':'earlyNoBtn'}}" bindtap="tryCount">{{canTryTime>0?'试算中，大约需' + canTryTime + 's': '点击试算'}}</view>
        <view class="earlyInfo">*试算金额仅供参考</view>
    </view>